# MugenForge Studio Handoff — v7.0 Authority Lab

Latest package: `mugenforge_studio_v7_0.zip`

Run:

```bash
python -m mugenforge.app
```

Install:

```bash
pip install -r requirements.txt
```

v7.0 adds/retains the late-project evidence stack: **Authority Lab**, **Evidence Core**, **Closure Lab**, **Gap Closer**, and **Authority Core**. These directly target the prior honest limitation list with source-level runtime simulation, external-engine adapter validation, SFF/SND corpus validation, unknown-binary triage, verified binary install/commit with rollback, gameplay tuning sheets, and evidence contracts.

Continue development from this package. Keep clean-room status. Do not claim bundled M.U.G.E.N/IKEMEN behavior or byte-identical full engine emulation. Treat mini-runtime outputs as source-level simulation. Treat external-engine logs and captured evidence as the strongest authority when configured.

Recommended next work:

1. Expand mini-runtime trigger/controller coverage.
2. Add more real-world SFF2/SND corpus fixtures and compatibility cases.
3. Add more engine profile templates for common M.U.G.E.N/IKEMEN builds.
4. Add visual UI readers for offline runtime traces, evidence contracts, and corpus results.
5. Reduce tab sprawl by merging Authority/Evidence/Closure workflows into a single late-project command center.
