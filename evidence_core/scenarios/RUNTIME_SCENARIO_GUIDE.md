# Runtime Scenario Sheet

Edit `runtime_scenario_sheet.csv` after playtesting. Use `status=pass`, `fail`, `blocked`, or `needs_tuning`, and attach evidence file paths when available.

Scenarios generated: 2
